import { ReactElement } from 'react';
import { PageLoaderContainer } from './styles';

export default function PageLoader(): ReactElement {
  return <PageLoaderContainer className="page-loader" />;
}
